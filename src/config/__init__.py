"""
config — Configuration and environment management.

Modules:
    settings — All constants (API keys, model name, paths) loaded from .env,
               plus the validate_keys() guard function.
"""
