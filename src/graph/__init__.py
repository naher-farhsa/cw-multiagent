"""
graph — LangGraph StateGraph definitions.

Modules:
    supervisor — State schema, Supervisor factory (make_supervisor_node),
                 and the inner supervisor_node closure.
    workflow   — build_workflow() assembles and compiles the full StateGraph
                 connecting supervisor, researcher, and writer nodes.
"""
