"""
src — Root package for the Content-Writing Multi-Agent System.

Sub-packages:
    agent  — LLM instance and ReAct agent definitions (researcher, writer).
    config — Environment settings, constants, and API-key validation.
    graph  — Supervisor logic and the LangGraph workflow assembler.
    tools  — LangChain @tool functions (web search, file I/O).
"""
